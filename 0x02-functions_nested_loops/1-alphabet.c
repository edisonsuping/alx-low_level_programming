#include "main.h"

/**
 * main - a function that prints the alphabet in lowercase
 * Return: Always (0) (success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		_putchar(c);

	_putchar('\n');
	return (0);
}
